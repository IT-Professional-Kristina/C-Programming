#include <stdio.h>

int main() {

    int a = 5;
    int b = 0;

    int result;
    result = a / b;

    printf("Result: %d\n", result);
    return 0;

/* runtime error program compiles but crashes while running A runtime error occurs while the program is running — after successful compilation.
That means:

The program compiles (no syntax errors).

It starts running.

But something goes wrong during execution — like dividing by zero or accessing invalid memory. */

}
